# ORION Frontend Environment Variables

# Base URL for n8n Webhooks (via Vite Proxy for local dev)
# USE /api-test-proxy to see executions in your n8n editor (Test Webhooks)
# USE /api-proxy for active production workflows
VITE_N8N_BASE_URL=/api-proxy

# Use this for Simulation/Testing if needed
# VITE_N8N_TEST_URL=/api-test-proxy